# PROJECT CHECKPOINT — siloverse messaging library

_Last updated: 2026-08-16. This document is the arbiter: if Claude asserts something about the code that isn't here or in a paste, call it — that's drift._

**Goal:** Learn EIP by building a messaging library. Modular monolith first, microservices later. Prior failure being avoided: a `message_delivery` table that reimplemented broker logic in PostgreSQL.

**Stack:** Java 21, Gradle (Kotlin DSL), version catalog `gradle/dep.versions.toml`, Spring Framework via BOM (NO Spring Boot), SLF4J api-only in core, JUnit 5 + AssertJ (AssertJ everywhere now, core included).

**Base package:** `io.github.siloverse.messaging`

**Git state:** `main` = release-cycle machinery landed via PRs #6–#8 (identity move + siloverse-build 1.8.0 + CI workflow + wrapper · `messaging-bom` · release guard), all through the branch-protection gate (required `build` check, up-to-date, linear history, rebase-merge only); `messaging-v0.1.0` released (PR #9, tag pushed); checkpoint recorded (PR #10); tag-triggered release workflow landed (PR #11, recorded #12); axion-release tag-derived versioning landed (PR #13, recorded #14); workflow-name typo fix (PR #15). `messaging-v1.0.0` + `messaging-v1.0.1` released via the one-command runbook. No branch open. Next act: direction = user's call (see Then / next).

## Locked decisions (do not reopen)

- Two unrelated markers: `Event` (0..many, pub-sub), `Command` (exactly 1, fire-forget). NO shared Message supertype. `Request<R>` designed then removed (YAGNI; blueprint in git history).
- Bus names: `SynchronousBus`, `AsynchronousBus`, `TransactionAwareAsynchronousBus`. No common supertype — guarantee visible at injection point.
- `SynchronousBus` = participant bus: same thread, same transaction, registration order, first failure rolls back everything. Zero event consumers = legal; zero command consumers = `NoHandlerException`. Participant→sync, observer→async.
- `@Consumer(id=...)` — id REQUIRED, never derived (queue name + consumer group + inbox dedup key).
- Fail-fast tiers: construction=NPE/IAE · registration/startup=`MessagingConfigurationException` · dispatch=`NoHandlerException`. Errors name culprit AND fix; one culprit per error (chained errors OK, e.g. lazy→then→parent-class).
- Registry lifecycle: OPEN → INITIALIZING → FROZEN. Lookups throw until FROZEN, registrations throw after it (TIGHTENED 2026-08-14, was lookups-legal-in-OPEN: registry is write-only while being built; `freeze()` = explicit construction-complete signal everywhere, unit tests included; OPEN and INITIALIZING now behave identically — INITIALIZING stays as the lifecycle marker + re-init guard). Armed in BFPP phase; frozen registry = broker topology source (`allConsumers()` + `MessageNameRegistry.allNames()` enumerations added 2026-08-14 for the declarer).
- `@Consumer` must be declared on the concrete class — parent-class declarations rejected at scan time (`rejectConsumersDeclaredOnParentClasses`). Single definition of "declares consumers" = `ConsumerMethodScanner.findDeclaredConsumerMethod(Class)` (class + superclass walk, returns `Optional<Method>`); Spring eligibility check delegates to it. NO validation-chain pattern (YAGNI — fixed policy, fail-fast, not user-extensible).
- Lazy/prototype bean WITH @Consumer (own OR inherited) = startup failure; without = skipped, laziness preserved.
- messaging-spring depends on core via `api(...)` (core types are part of its public surface).
- Plain `@Configuration` (`MessagingConfiguration`, main source set), static `@Bean`s for the BFPP chain.

## Built & green

- **messaging-core** (slf4j-api only): markers · 3 bus interfaces · `MessageKind` · `ConsumerDefinition(id, messageClass, bean, method, contextParameterIndex=-1)` · `ConsumerRegistry` (3-state, cardinality, dup-id, no-partial-state) · `ConsumerMethodScanner` (id non-blank, 1 param, one marker, void return, concrete-class-only + `findDeclaredConsumerMethod` helper) · `MessageDispatcher` (InvocationTargetException unwrap triage) · `DefaultSynchronousBus` · full AssertJ unit suites (48 tests green).
- **messaging-core async chapter (2026-08-13):** `naming/MessageNameRegistry` (builder, freeze, dup-class + dup-wire-name + blank rejected, culprit-and-fix messages) · `transport/Envelope` (record; nulls rejected at construction, `Map.copyOf` + `clone()` in/out defensive copies; equality-by-reference on payload accepted as YAGNI) · ports `transport/MessageTransport.send(Envelope)` + `transport/PayloadSerializer` (serialize + contentType) · `dispatch/DefaultAsynchronousBus` (mints UUID per dispatch, registry name, content-type header, shared private `dispatch(Object)`; unregistered class throws before transport). Tests use nested fakes in `DefaultAsynchronousBusTest` (FakePayloadSerializer = toString bytes, RecordingTransport) — promote to fixtures on second use.
- **messaging-spring**: `SpringConsumerScanner` (unwrap → delegate → selectInvocableMethod) · `ConsumerScanningInitializer` (BFPP arms once; SmartInitializingSingleton scans→freezes; eligibility inspects class metadata incl. superclasses, never instantiates) · `MessagingConfiguration` · integration tests green incl. @Transactional CGLIB end-to-end, @PostConstruct-publish startup failure, laziness preserved, lazy-inherited-consumer startup failure.
- **messaging-spring outbox (2026-08-14):** `outbox/JdbcOutboxWriter(JdbcTemplate, ObjectMapper)` — inserts the four bus-authored columns, `CAST(? AS jsonb)` for headers, `JsonProcessingException` wrapped in `MessagingException` (base type: not config, not dispatch) with messageId/type culprit. DDL shipped as resource `io/github/siloverse/messaging/outbox-postgres.sql` (table `messaging_outbox`; no UNIQUE on message_id — duplicates impossible by construction; contrast future inbox where UNIQUE IS the dedup mechanism). Testcontainers-Postgres integration tests apply the shipped DDL (file can't rot) and prove: commit keeps row with published_at NULL, ROLLBACK LEAVES NO ROW, no-tx append still writes. Decisions: real Postgres over H2 (jsonb/bytea in write path from day one — a lookalike DB is a mock in costume); DDL-as-resource, NO Flyway/Liquibase dependency (schema ownership belongs to the app; test-applies-shipped-file keeps it honest); Jackson as `api` dep (ObjectMapper in public constructor — same rule as core-via-api), ObjectMapper injected never owned (library inherits app config; safe because headers are a flat string map — fence in javadoc); jsonb lesson: Postgres stores parsed structure and re-renders text its own way — assert data, not formatting.

- **messaging-rabbitmq (2026-08-14):** module created — bare `com.rabbitmq:amqp-client`, NOT Spring AMQP (decided: no abstraction overlap with Envelope/PayloadSerializer/MessageNameRegistry; blocking confirm maps directly onto the `send` contract; Spring AMQP hides the AMQP model this project exists to learn; adopting its listener container later JUST for the consumer side stays open). Build architecture learned: siloverse-build's convention plugin applies the shared platform (spring-boot-dependencies + testcontainers + junit BOMs) to `implementation` AND `testImplementation` — catalog entries stay VERSIONLESS, platform governs versions, never pin what a BOM manages. **Catalog placement rule (litigated, settled):** shared catalog = deps services are blessed to declare directly. `testcontainers-rabbitmq` went shared (siloverse-build 1.7.0); `amqp-client` stays in this repo's LOCAL catalog — sharing it would advertise bypassing the buses. Testcontainers 2.x renamed artifacts (`testcontainers-rabbitmq`, package `org.testcontainers.rabbitmq`).
- **Consumer/listener chapter built & green (2026-08-15) — messages now flow END TO END: committed tx → outbox → relay → broker → queue → listener → typed event in an `@Consumer` method (crown test upgraded to prove exactly that; rollback test proves the worker is NEVER invoked).** Pieces:
  - **Failure policy (gate closed): never lose, never loop.** On consumer exception: first delivery → nack requeue=true (ONE immediate retry, covers transients); redelivered → nack requeue=false → parks in `<queue>.dlq`. Bounded at two attempts. DLQ = where messages go to PAGE someone, not to die (ERROR log with message id + cause + replay instruction). REJECTED: ack-regardless (smuggles at-most-once), unbounded requeue (poison hot-loop blocks the queue), time-based two-day cutoffs (attempts are the native axis; a failure surviving minutes of retries needs a human — days of retrying just delays the page). Backoff = infrastructure RabbitMQ doesn't provide natively (TTL wait-queue ladder) — parked as a THROUGHPUT/latency refinement; the litmus that split v1 from parked: "if we never built it, would a message be lost or stuck?" Park features, never guarantees.
  - **Declarer: dead-letter wiring** — every consumer queue gets `x-dead-letter-exchange: ""` + `x-dead-letter-routing-key: <queue>.dlq` (default-exchange routing, no extra exchange objects) and a durable terminal `.dlq` (no dead-letter args of its own — no loops). Parking lot declared BEFORE the queue that points at it: no state may exist where a nack routes to a missing queue. Test proves nack-without-requeue parks with identity + `x-death` intact.
  - **`listener/RabbitMqMessageListener`** (bare client): channel per consumer, `basicQos(1)` (unset prefetch = unbounded deliveries into heap — the QoS call is memory-safety, tuning is parked), manual acks. Per delivery: verify routing-key type via `classOf` against the definition (topology-corruption fail-fast; the definition already knows its class — queue-per-consumer makes `classOf` a check, not a necessity), deserialize, dispatch via core `MessageDispatcher` (INJECTED — no self-creation of collaborators, user rule; single definition of dispatch incl. Error passthrough), ack. NOTHING escapes the client's consumer thread. Partial-start failure closes already-opened channels (no half-started listeners). Connection BORROWED (the separate consume connection — broker flow control throttles publishers, must not starve consumers), channels OWNED. Lesson paid for in red: `Method.invoke` accessibility is checked against the CALLER's package — `ConsumerMethodScanner.setAccessible(true)` at scan time IS the registration contract; hand-built test definitions must mirror it.
  - **Core additions:** `MessageNameRegistry.classOf(String)` (O(1) via reverse map DERIVED in the ctor from `names` — one code path serves freeze and compose; unknown wire name → config error: "compose the publishing silo's contract jar", never null — null smuggles the failure to dispatch where the culprit is lost) · `PayloadDeserializer` port (`<T> T deserialize(byte[], Class<T>)` — generic for cast-free dispatch; the port MUST live in core: the listener is in messaging-rabbitmq which depends only on core) · `JacksonPayloadDeserializer` (spring, beside the serializer; type named in errors, payload bytes NEVER).
  - **`listener/MessageListener` hook (spring)** — start/stop mirror of `TopologyDeclaration`; app assembly wraps the broker listener AND owns the consume connection (opened on start, closed on stop — an internal detail of the listening machinery, NOT a context bean: avoids two-`Connection`-beans qualifier ceremony). `MessagingLifecycle` order: declarations → listeners → relay on start; listeners FIRST on stop, then relay, before any singleton destruction.
- **Spring wiring built & green (2026-08-15) — the async chapter CLOSES on the publish side.** Crown integration tests (`MessagingWiringIntegrationTest`, real Postgres + real RabbitMQ, full Spring context): committed tx → outbox → relay → broker → consumer queue (payload/messageId/persistence intact, row stamped); rolled-back tx → nothing anywhere. Pieces:
  - **`config/AsyncMessagingConfiguration`** — buses (`DefaultAsynchronousBus`, `DefaultTransactionAwareAsynchronousBus`), `JdbcOutboxWriter`, `OutboxRelay`, `MessagingLifecycle`. SPLIT from `MessagingConfiguration` (sync+scanning only) after the full build caught the regression: unconditional async beans broke sync-only contexts. No Boot = no conditionals; explicit opt-in = register BOTH config classes; sync-only apps register one. App-side contract for async: `MessageTransport`, `PayloadSerializer`, composed `MessageNameRegistry`, `JdbcTemplate`, `ObjectMapper`, broker-specific `TopologyDeclaration`, `@Bean(destroyMethod="close") Connection`.
  - **`config/MessagingLifecycle`** (`SmartLifecycle`): start = declare all `TopologyDeclaration` beans THEN schedule relay (a tick before declaration would 404 into a missing exchange); Spring guarantees start runs after `SmartInitializingSingleton` freeze and stop runs before singleton destruction → relay quiet before Connection closes. Library-owned single-thread `ThreadPoolTaskScheduler` (`outbox-relay-`), created on start/destroyed on stop. Tick failure = EXPLICIT catch-log-continue (next tick IS the retry; never rely on Spring's default repeating-task error handler implicitly).
  - **`topology/TopologyDeclaration`** — broker-agnostic one-method hook; app wraps its adapter's declarer; failures abort boot.
  - **`outbox/OutboxRelaySettings`** — optional bean, `DEFAULT` = 1s fixed delay on the record itself, resolved via `ObjectProvider.getIfAvailable` (the plain-Spring default idiom).
  - **`serialization/JacksonPayloadSerializer`** — `writeValueAsBytes`, `application/json`, ObjectMapper injected-never-owned; error names the TYPE, NEVER the payload (business data in logs — same rule as never printing the password).
  - **Core: `MessageNameRegistry.compose(MessageNameRegistry...)`** — replays registration through a fresh builder, so all dup checks apply across contract jars (two jars claiming one wire name, or a doubled jar, die at startup naming culprits).
  - **Convention (litigated, settled 2026-08-15): settings records live in the feature package of the component that consumes them.** NO `config`/`settings` kind-packages — package-by-feature, things that change together live together. Discoverability of the app contract belongs to javadoc/README, not package names.
- **`connection/RabbitMqConnectionSettings` + `connection/RabbitMqConnector` built & green (2026-08-14):** validating record (NPE/IAE tier, blank + port-range checks) + stateless `final` class, static `connect(settings)` = pure function returning a NEW connection each call — NO static caching (once-ness is the wiring layer's job via bean scope; a static cache would ignore settings after first call and poison tests). Recovery policy set explicitly (`setAutomaticRecoveryEnabled` + topology recovery); test proves it mechanically via `instanceof Recoverable` (client returns `AutorecoveringConnection` exactly when recovery is on). 7 tests: 4 unit (validation), 3 integration (connects+recoverable, wrong credentials → config tier without leaking the password, unreachable broker → base tier via fast connection-refused on port 1).
- **`topology/RabbitMqTopologyDeclarer` built & green (2026-08-14), 5 integration tests + 3 core unit tests:** `declarePublisherTopology(MessageNameRegistry)` = durable fanout exchange per `allNames()`; `declareConsumerTopology(serviceName, ConsumerRegistry, MessageNameRegistry)` = per `allConsumers()`: `nameOf` (unregistered consumed class → config error at startup binding, culprit+fix free from the name registry) → exchange → durable/non-exclusive/non-auto-delete queue `<service>-<consumer-id>` → bind (routing key = wire name). Fresh channel per declaration run, closed after (startup-time — churn rules don't apply). 406 precondition-failed (inequivalent redeclaration) → `MessagingConfigurationException` with broker's reason text; other failures → base tier; every failure names the exact exchange/queue/binding. Tests prove: end-to-end registry→declarer→transport→queue, zero-consumer publish safe after publisher declaration, unregistered-class startup failure, idempotency (twice + both sides), 406 → config tier. Core gained `ConsumerRegistry.allConsumers()` + `MessageNameRegistry.allNames()` (lookup-gated).
- **`transport/RabbitMqMessageTransport` built & green (2026-08-14), 3 integration tests (Testcontainers RabbitMQ, real broker):** happy path (no-sleep `basicGet` right after `send` — the timing IS the blocking-guarantee assertion; payload/messageId/headers/delivery-mode-2 asserted), missing-exchange fails loudly instead of hanging (send on separate thread + bounded get, so a hang fails the test not the build), dead-connection throws `MessagingException` not client internals. Design: confirm-mode channel opened in ctor; `ConcurrentSkipListMap<Long, PendingMessage(envelope, future)>` outstanding map (skip-list because `multiple=true` acks/nacks drain `headMap`); publish section synchronized (seqNo + map-put + basicPublish MUST be atomic — misalignment completes the wrong futures); blocking `send` waits on its OWN future (per-message futures, 30s bound) so concurrent senders pipeline on one channel; `ShutdownListener` drains ALL outstanding futures on channel death (the widow-maker fix — without it blocked callers hang forever); ALL client exception types translated to `MessagingException` (`AlreadyClosedException` is a `ShutdownSignalException`, a RuntimeException — catch it explicitly). `publish` kept PRIVATE (it IS sendAsync; ships only when decided). PendingMessage retains the envelope so failures name the culprit.

## RabbitMQ adapter — decisions locked (2026-08-14)

- **Connection ownership:** services provide CONFIG only (host, port, username, password); the library handles connections internally. Two layers: the connector (settings record → configured auto-recovering `Connection`; the WIRING owns closing — Spring bean `destroyMethod="close"` later) and the transport (borrows `Connection`, owns its `Channel` + confirm state). Constructor-does-I/O is deliberate: broker unreachable = loud boot failure, same philosophy as registry freeze in BFPP. Connection must be owned ABOVE the transport because the future consumer side needs channels on the SAME connection (RabbitMQ model: one connection, many cheap channels).
- **One shared transport instance** for `DefaultAsynchronousBus` + `OutboxRelay`. Interleaving at message granularity is harmless (independent streams; relay ordering is within-its-own-sequence and it's single-threaded). Splitting instances = wiring qualifiers bought against unmeasured contention; two-line change if ever measured.
- **Declaration ownership (settled 2026-08-14):** each side declares what it TOUCHES — publishers declare exchanges (one per name in their frozen `MessageNameRegistry`; idempotent by AMQP contract), consumers declare exchanges + their queues + bindings. Resolves the zero-consumer conflict: without publisher declaration, a LEGAL zero-consumer event would 404 and kill the transport. The empty exchange does two jobs: publisher's stable public address (consumers bind/unbind freely) and the broker-enforced boundary between "nobody cares" (acked no-op, legal) and "no such message type" (404, loud). Queues belong to consumers ONLY — an unconsumed queue accumulates forever; an idle exchange is a free routing-table entry. NEW CONFIG KNOB: the consuming service must supply its service name (queue prefix) — parameter on `declareConsumerTopology` for now, promoted to a settings object when a second consumer of it appears.
- **Topology: exchange-per-message-type, fanout; exchange name = routing key = wire name from `MessageNameRegistry`.** Chosen over one big topic exchange for fail-fast: typo'd type → 404 channel error → send throws; in a topic exchange a typo'd routing key silently drops. Binding = subscription (what consumer startup declaration will do). Routing key is informational under fanout (wire self-description, uniform column mapping, forward-compat) — if exchange and key ever differ, someone changed an exchange type and the transport mapping needs revisiting.
- **`mandatory` flag NOT set — decision, not oversight.** Unroutable = acked and dropped, correct because zero-consumer events are LEGAL; mandatory+ReturnListener would flag legal events as failures and drag kind-awareness into the kind-agnostic publish path (returns carry no seq numbers — correlation would need messageId matching). Command-must-have-consumer is enforced at consumer-startup topology declaration, not in the publish path.
- **Failure contract is ASYMMETRIC (javadoc + every error message teaches it):** return = 100% durably published (persistent message, durable-queue ack). Exception = NOT-CONFIRMED-published, two flavors the messages distinguish: local publish failure = "not published, retry"; timeout/interrupt/channel-lost = "outcome UNKNOWN — treat as unpublished and retry (consumers dedup by message id)". The symmetric version ("exception = certainly not published") is exactly-once and does not exist.
- **Exception tiers at this layer (settled 2026-08-14):** tier is chosen by WHAT THE FIX IS, not when it fires. Channel-open failure at transport construction = `MessagingException` (environmental). Connector: `AuthenticationFailureException` → `MessagingConfigurationException` naming host/port/username, NEVER the password (fix = correct config); other `IOException`/`TimeoutException` → base `MessagingException` (fix = operational). `MessagingConfigurationException` gained a `(String, Throwable)` ctor for this.
- **Channel-death policy gap (recorded, undecided):** client auto-recovery (on by default) heals CONNECTION-level failures — sends fail during outage, work after, relay retries free. It does NOT resurrect channels closed by protocol errors (the 404): after that this transport is dead until restart. Defensible (broken topology = loud permanent failure) — decide policy when topology declaration lands.
- **Throughput ladder (parked, in order):** batched confirms for the relay (publish N, await all — needs port widening + stop-at-first-failure rethink) → `sendAsync(Envelope)` → `CompletableFuture<Void>` (completion = durable ack, same contract deferred; impl is already there privately; javadoc must fence blocking in callbacks — they run on the connection I/O thread) → channel pool (only if stuck blocking AND concurrent; honest competitor then is Spring AMQP's CachingConnectionFactory) → separate publish/consume connections (about broker flow control, not throughput: memory-pressure throttling of publishing connections must not stall consumers — adopt when consumer side lands). REJECTED: channel-per-call (4 round trips per message + broker channel churn), connection pool (AMQP connections multiplex — pooling them is a JDBC habit), ThreadLocal channels (leaks + virtual-thread explosion).
- **Not integration-testable, accepted:** real broker nack (only on internal broker failure — translation covered via shutdown/dead-connection paths); lost-ack-on-healthy-channel (needs Toxiproxy — parked); publish-lock atomicity (reviewed, not tested — no nondeterministic concurrency tests).

## Async chapter — design state (agreed, not yet coded)

**Outbox table (question a — answered):**

| column | type | job |
|---|---|---|
| `id` | bigserial | relay polling order (NOT created_at — ties) |
| `message_id` | uuid | wire identity; travels in headers; consumer inbox dedup key later |
| `message_type` | varchar | routing — picks the exchange (from `MessageNameRegistry`, never the class name) |
| `payload` | bytea | sealed bytes; relay NEVER deserializes |
| `headers` | jsonb | wire metadata: content-type, schema version, trace context |
| `created_at` | timestamptz | diagnostics only |
| `published_at` | timestamptz null | stamped by relay after broker ack; NULL = pending |

Relay mapping: `basicPublish(exchangeFor(message_type), message_type, propsFrom(headers, message_id), payload)` — every argument a column read.

**Forbidden columns (question b — answered):** `consumer_id`/recipient (broker bindings' job), per-consumer delivery status (broker acks), `retry_count`/`next_retry_at` (broker redelivery + DLQ), `processed`/`handled_at` (consumer's inbox table, consumer's DB), extracted business fields (payload is sealed). Litmus: outbox knowledge ends at broker ack; anything after = rebuilding `message_delivery`.

**Row fate (question c — decided):** relay ALWAYS stamps `published_at`. Hard delete / archive = future standalone retention job consuming the stamp (janitor cron), NOT a relay concern. NO strategy interface (litigated twice; the stamp is the extension point).

**RabbitMQ model (lesson locked):** publishers target EXCHANGES, never queues. Queue-per-consumer-id `<microservice>-<consumer-id>`, declared + bound by the CONSUMING side at startup from its frozen registry. Relay publishes once; broker fans out. Relay is kind-agnostic — event/command difference lives in topology declaration at boot, not in the publish path.

## ✅ Gate closed — crash story answered (2026-08-13)

Unstamped row after relay restart is ambiguous (never-published vs published-but-unstamped look identical; relay can't ask the broker). Relay REPUBLISHES — duplicate beats loss. Consumer sees byte-identical duplicates, same `message_id` = dedup key.

**`TransactionAwareAsynchronousBus` contract (javadoc must promise):** committed tx ⇒ delivered **at least once**; duplicates possible; consumers must be idempotent; rolled-back tx delivers nothing (outbox row rolls back with business data). Exactly-once delivery = myth without 2PC; real thing is at-least-once + idempotent consumption.

**`AsynchronousBus` rationale (decided):** direct-to-broker, NO outbox/relay/DB in path. Exists for (1) stateless publishers (no tx to join — outbox structurally unusable), (2) self-healing messages (cache hints, telemetry, heartbeats) not worth the outbox premium. Litmus: message asserts a DB-committed fact → tx-aware bus; otherwise → async bus. Contract: **at-most-once** wrt crashes (may await broker ack, throw on nack; crash before ack = silent loss). Ghost-message trap: publish inside a rolled-back tx announces a state that never happened — javadoc warns.

**Message naming — LOCKED, do not reopen (litigated 6×; 5× 2026-08-13, reopened 2026-08-14 as "class FQN as routing key, renames managed by rollout planning + team-lead review" and HELD: messages in flight outlive deploys — outbox rows and queued messages carry the old name after a rename, and the no-`mandatory` acked-and-dropped design makes the resulting loss SILENT and review-proof; the registry makes class renames a non-event instead of a distributed migration):** wire name comes from a `MessageNameRegistry` (core class: builder, `register(Class, String)`, `freeze()`, `nameOf(Class)`). Each service ships a static registry INSIDE its messages/contract jar (e.g. `OrderSiloMessages.names()`) so publisher and consumers read the same map — classes and names travel together. Name convention: `<service>.<message>` lowercase-dashed (e.g. `order-silo.order-created`) — a style rule for humans, never computed. REJECTED: `@MessageName` annotation (user preference; delete the typed file), class-name derivation (dash illegal in packages, rename = silent loss — failed twice in user's own examples), instance-level MessageProvider naming (no instance exists at startup binding time). Wire identity must be static per type, resolvable from `Class` alone. Failures: unregistered class → `MessagingConfigurationException` at first publish (publisher) / at startup binding (consumer); blank name or duplicate class → builder throws. Service module layout agreed: `silo` (app) / `messages` (contract jar) / `web` (DTOs); dashes legal in Maven coordinates, not in Java packages. "MessageProvider" = app-side factory building message objects — user's own code, no library involvement; bus signature stays `publish(Event)`/`send(Command)`.

**Ghost-trap enforcement (decided):** NO runtime tx detection — trap is semantic (message meaning), not mechanical (tx active), so a runtime guard false-positives on legit uses (e.g. metrics inside `@Transactional`) and would drag spring-tx into the publish path. Enforcement = javadoc + code review; static-analysis suggestion parked.

## Then / next

**Relay — design locked (2026-08-14), built & green:** `OutboxRelay(JdbcTemplate, MessageTransport, ObjectMapper)` in messaging-spring outbox package; `pollOnce()` = SELECT pending ORDER BY id → rebuild Envelope (headers parsed back, payload bytes sealed/untouched) → `transport.send` → stamp per row. STOP at first send failure (failure is usually broker-wide; preserves order; retry-for-free = unstamped row + next tick). Driven by Spring `TaskScheduler` with `scheduleWithFixedDelay` (no overlap by construction). `MessageTransport.send` contract STRENGTHENED and documented on the port: returns only after durable broker acceptance (RabbitMQ publisher confirm), throws `MessagingException` otherwise — return = permission to record as published. Relay built & green (3 integration tests incl. stop-and-resume-at-failed-row).

**Idempotency stance (decided 2026-08-15, corrects an earlier framing):** the inbox is NOT a guarantee — the locked contract already says "duplicates possible; consumers must be idempotent," which is honest WITHOUT dedup machinery. Default: consumers declare idempotency by existing; the obligation is enforced like the ghost-trap — javadoc + review (checklist question: "would this method running twice with the same message change the outcome twice?" — beware increments/appends/inserts-per-event; setting absolute state is safe). Dedup is an OPT-IN convenience for handlers that can't be idempotent.

**`@Consumer(dedup = true)` built & green (2026-08-15, Claude-implemented end-to-end by user delegation):** per-consumer opt-in dedup, 9 new tests (107 total). Pieces: annotation attribute (default false; javadoc carries the idempotency checklist) → `ConsumerDefinition.dedup` (compat 5-arg ctor keeps old call sites; BOTH scanners carry the flag — SpringConsumerScanner's proxy-rebuild path too) → core port `consumer/Inbox.processOnce(consumerId, messageId, invocation)` (port in CORE because the listener knows no databases — same forcing as PayloadDeserializer) → `spring/inbox/JdbcInbox(JdbcTemplate, TransactionTemplate)`: ONE tx holds ledger insert + invocation; `ON CONFLICT DO NOTHING` (a raised unique violation ABORTS a Postgres tx — the conflict clause reports count 0 instead; concurrent duplicate blocks on the index until the first tx resolves); invocation throws → tx rolls back → ledger entry gone → redelivery passes the gate (tested). DDL resource `inbox-postgres.sql`, PK `(consumer_id, message_id)` — per-consumer dedup tested. Listener: dedup definitions route dispatch through the inbox, duplicate = debug-logged + ACKED like a processed message; `dedup = true` with NO inbox provided = startup config error naming consumer + fix (tested); non-dedup path untouched. Wiring crown suite: byte-identical duplicate over the real broker → dedup consumer invoked ONCE, one ledger row. Guarantee shipped: at-least-once invocation, exactly-once COMMITTED effects; extra-tx side effects remain the handler's (tx-aware bus = escape hatch). Semantic duplicates (same intent, different message_id) out of scope — business idempotency.

**Next: library is functionally complete for the modular-monolith story** — publish durably (outbox), consume idempotently (obligation by default, `dedup = true` machinery opt-in), both sides honest about at-least-once. **Direction chosen (2026-08-15): the release cycle** (section below) — it also unblocks the example silo app, which needs published artifacts to consume. Still queued behind it: retention janitor (outbox stamped rows + inbox ledger both grow forever — first shared refinement) · the example silo app itself · parked refinements when measured need appears.

## Release cycle — decisions locked (2026-08-15)

**Repo model: `java-library` is a CONTAINER, not a library.** Each top-level directory (`messaging/` today; pagination/security/etc. if they earn existence) is one library with its own version, own release cadence, own tags. Signal to split a library into its own repo: it needs its own release cadence or ownership — until then, one repo, no coordination tax.

- **Library identity lives in the library directory:** `messaging/build.gradle.kts` = `subprojects { group = "io.github.siloverse"; version = libraryVersion }` (since 2026-08-16 `libraryVersion = scmVersion.version`, tag-derived — see rung 2). NO per-module version lines (litigated by a live failure: a module's own `version =` runs after the parent block and silently wins — rabbitmq/spring published `1.0.0` beside core's `0.1.0-SNAPSHOT` on day one). NO root gradle.properties version (couples future sibling libraries into false lockstep). Traps recorded: Gradle only auto-reads gradle.properties at the BUILD root — `messaging/gradle.properties` would be silently ignored; default group is `<rootName>.<parentPath>` (the container leaked into coordinates as `java-library.messaging` until group was set).
- **Lockstep WITHIN a library, emphatically:** all messaging modules share one version incl. unchanged ones (republishing costs nothing; a compatibility matrix costs forever). Consumer-facing face = `messaging-bom` (`java-platform` module, BUILT, PR #7): silo declares `platform("io.github.siloverse:messaging-bom:x.y.z")` + versionless modules. BOM hand-wires `maven-publish` + the GitHubPackages repo (mirroring the convention plugin's chain: `siloverse.publish.repository` prop → `GITHUB_REPOSITORY` env → fallback; creds `gpr.user`/`GITHUB_ACTOR` + `gpr.key`/`GITHUB_TOKEN`) because the jvm-library convention doesn't fit a platform (no jar; conflicting components) — a `siloverse.platform` convention is the natural extraction to siloverse-build. Bonus job: constraints reference every module via `project(...)`, so a module silently dropped from the build fails loudly (caught a real settings.gradle.kts edit slip that had overwritten the messaging-spring include — a duplicate `include` is legal and silent, a missing module only fails when referenced). ~~Version stays `0.x` until a second real service survives an upgrade~~ — SUPERSEDED 2026-08-16: user released `1.0.0`/`1.0.1` (release-time call, immutable now). Consequence accepted: semver is full-strength — breaking changes require a MAJOR bump from here on.
- **Version is NEVER written in a file (SUPERSEDED 2026-08-16 by rung 2; was: always `x.y.z-SNAPSHOT` in the repo):** axion derives it from git — bare `x.y.z` exists only when HEAD sits exactly on a clean `messaging-vx.y.z` tag; everywhere else it's next-patch `-SNAPSHOT`. The invariant the old rule protected (snapshots everywhere except the release moment) now holds by construction instead of by discipline.
- **Registry (decided 2026-08-15, reverses the initial Maven Central recommendation): GitHub Packages** — every consumer is ours and already authenticates there for the siloverse-build plugins (settings.gradle.kts credential block; `GITHUB_ACTOR`/`GITHUB_TOKEN` fallback = CI-ready for free); Central's ceremony (namespace verification, GPG, javadoc jars) buys distribution to strangers who don't exist. **Immutability is POLICY here, not mechanism** (GHP allows delete/republish; Central doesn't): a released `x.y.z` is never deleted or republished — bad release ⇒ `x.y.z+1`. Ghost-trap-class rule (javadoc/README + review). Central PARKED, trigger: first consumer outside the GitHub account (nothing lost — republish from tags).
- **Snapshot testing:** `cd messaging && ../gradlew publishToMavenLocal` (directory-scoped task execution is built into Gradle — the "publish-test at my directory level" goal costs nothing) → consuming silo adds `mavenLocal()` + explicit `-SNAPSHOT` pin, BOTH temporary scaffolding that never ships on its main branch. Snapshots never leave the machine; re-publish overwrites freely.
- **CI (BUILT, PR #6 `.github/workflows/build.yml` + branch protection clicked): one PR check = `./gradlew build`** (Gradle's build already IS compile→test→check; Testcontainers runs on GitHub-hosted runners, Docker preinstalled). Branch protection on `main`: required check + required up-to-date-with-main (mechanical "rebase first"; CI re-runs against what actually lands) + linear history/rebase-merge only. Nothing publishes from CI; merge and release are decoupled (merge five PRs, release once). Tag-triggered CI release job = parked refinement; v1 releases publish from a local machine.
- **Release shape — option (a), LOCKED after full tag-mechanics litigation:** release PR carries BOTH bump commits (`x.y.z` + next `-SNAPSHOT`), rebase-merges like any PR; tag `messaging-vX.Y.Z` applied to the release commit on `main` AFTER merge (a tag never travels through a PR — rebase-merge rewrites SHAs and orphans pre-merge tags; GitHub's rebase button rewrites even already-rebased branches). REJECTED: release-bubble merge commits (option b — sound git, but requires disabling "require linear history" repo-wide, making merge-style-per-PR a human-discipline rule; we convert those to mechanical ones). Tags are per-library (`messaging-v...`) — a bare `vX.Y.Z` is ambiguous in a container repo.
- **Publish = deliberate human act from a local machine, guarded mechanically (BUILT, PR #8):** `releaseGuard` task in `messaging/build.gradle.kts` (the identity file — guard is identity enforcement); every subproject task of type `PublishToMavenRepository` depends on it, which covers ALL remote publishes and EXCLUDES `publishToMavenLocal` (sibling class) — snapshots stay frictionless, nothing leaves the machine unguarded, nobody has to remember to run it. Four checks, each error names culprit+fix: version not `-SNAPSHOT` → tree clean (`git status --porcelain`) → HEAD tagged `messaging-v<version>` → HEAD ancestor of `origin/main` (`merge-base --is-ancestor`, exit code as verdict via `isIgnoreExitValue`). `libraryVersion` hoisted to one val shared by identity block and guard (two literals would drift). Git state read via lazy `providers.exec` declared at config time, evaluated in `doLast`. Red-verified: snapshot publish attempt died on check 1 before any network; `publishToMavenLocal` unaffected. Green path exercised by the first real release. NOTE: local publish needs `gpr.key` PAT with `write:packages` scope (read-only token → 403). REJECTED: interactive Gradle tasks (prompting fights the tool, breaks CI/config-cache — version is passed or read from file, never asked) · any git-wiping `:publish:clean` task (build tasks never destroy git state; keep the guard, lose the cleaner).
- **One release = one version across N artifacts, never one jar:** jar+pom per module (+ BOM, pom-only). Module boundaries are dependency boundaries (core = slf4j only; a fat jar would smuggle Spring and amqp-client onto every classpath — the catalog-placement rule's whole point). Consumers pick modules; the BOM makes it feel like one thing.
- **Build-JVM vs toolchain lesson (paid 2026-08-15):** conventions `1.7.0` was published requiring build-JVM ≥25 while the stack targets Java 21 — a plugin's JVM floor propagates to every consumer's Gradle launch (distinct from the toolchain, which still compiles at 21). Fixed in siloverse-build `1.8.0` (which also brought the `mavenJava` publication + GitHubPackages repo wiring).
- **Ownership path:** prototype the release machinery HERE (user's explicit call, overriding the build-machinery-belongs-to-siloverse-build default), extract to the convention plugin once proven.

- **Gradle wrapper is COMMITTED (`gradlew` + `gradle-wrapper.jar`; lesson from CI exit 127 — a template .gitignore was hiding it):** the wrapper exists precisely to be committed (pinned 9.6.0 from tracked `gradle-wrapper.properties`; runner's preinstalled Gradle is an unpinned moving target); `setup-gradle` validates the jar checksum every run (neutralizes binary-in-git objection). `gradlew.bat` DELIBERATELY stays ignored (all-Linux shop; `gradle wrapper` regenerates it as a set — the ignore line keeps it out of `git status` forever; one-line revert if Windows ever joins). `.gitignore` must never ignore tracked files (force-add creates a file that lies).
- **CI auth fact:** `secrets.GITHUB_TOKEN` is minted per run by Actions (repo-scoped, expires with the job), must be explicitly mapped into step `env:`; settings.gradle.kts's `gpr.key → GITHUB_TOKEN` fallback serves laptop and CI from one file. Cross-repo package READ works because siloverse-build is public (public packages accept any valid token; private would need package-settings access grant). Same token could WRITE only this repo's packages — relevant for the parked tag-triggered CI release job.

**✅ `messaging-v0.1.0` RELEASED (2026-08-16)** — the full runbook executed clean: release PR with both bump commits (`7db741d Release messaging 0.1.0`, `6a7891f Begin messaging 0.2.0 development`) through the gate, rebase-merged; tag `messaging-v0.1.0` on the release commit (`HEAD~1`); published from the tag checkout — guard's four checks passed (its first green), all four artifacts (core, spring, rabbitmq, bom) live on GitHub Packages at `0.1.0`. Repo version now `0.2.0-SNAPSHOT`. The release cycle is PROVEN end to end. (The manual publish half of that runbook — checkout tag, local `../gradlew publish`, `write:packages` PAT — was superseded 2026-08-16 by the rung-1 CI workflow below; the release-PR + tag half stands.)

**Release automation ladder (decided 2026-08-16, after running the manual runbook once — "too much manual work" is the trigger we parked against):**
- **Rung 1 ✅ BUILT & SMOKE-TESTED (2026-08-16, PR #11): tag-triggered CI release workflow.** `.github/workflows/release.yml`: on push of tag `messaging-v*`, the runner checks out the tag and runs `cd messaging && ../gradlew publish` (`working-directory` scoping — container repo: a messaging tag must never publish a future sibling library). `releaseGuard` stays the brain — same four checks, executed by the runner. Deltas from build.yml, each litigated: `permissions: contents: read, packages: write` (least privilege stated in the file; auto-minted `GITHUB_TOKEN` writes THIS repo's packages — local `write:packages` PAT and the checkout-tag-publish-checkout-back dance DELETED) · `fetch-depth: 0` on checkout (default shallow single-ref fetch has neither `origin/main` nor history, so guard check 4's `merge-base --is-ancestor` fails with a LYING error — "not on main" when the runner just can't see main) · `GITHUB_TOKEN` mapped into step env (feeds the settings.gradle.kts `gpr.key → GITHUB_TOKEN` fallback; `GITHUB_ACTOR` is in the Actions env for free). Facts learned: the workflow file must be on the DEFAULT BRANCH before tag pushes trigger it (smoke test only possible post-merge) · smoke test = junk tag `messaging-v0.0.0-smoke` on snapshot HEAD → workflow fired, guard check 1 refused in 48s before any network — red for the predicted reason proves trigger/permissions/checkout/toolchain/creds/guard-on-runner; tag deleted from both sides after (a leftover tag in the release namespace lies) · checks 3/4 (tag-at-HEAD, ancestor — the fetch-depth fix) remain proven by reasoning only; first green = the real 0.2.0 release, same pattern as the guard itself · do NOT smoke-test past check 1 by tagging the old release commit — that passes ALL FOUR checks and republishes, straight into the immutability policy. **Runbook now:** release PR (both bumps) → merge → tag `messaging-vX.Y.Z` on the release commit → push tag → CI publishes. Release PR with bump commits stays until rung 2's trigger fires.
- **Rung 2 ✅ BUILT (2026-08-16, PR #13, pulled forward — the trigger effectively fired when one manual release plus a proposal to script the bump commits landed the same day; scripting them would have been rung 1.5, machinery rung 2 deletes): axion-release** (`pl.allegro.tech.build.axion-release` 1.21.3, local catalog — repo machinery, not a blessed service dep). Version derived from git: HEAD exactly on a clean `messaging-vX.Y.Z` tag = bare `X.Y.Z`; anything else = next-patch `-SNAPSHOT`. Identity file: `libraryVersion = scmVersion.version`; guard survives intact as cross-checks (check-1 fix text updated for tag-first process). Config, each litigated: `prefix = "messaging-v"` + `versionSeparator = ""` (default separator would look for `messaging-v-0.2.0` and silently miss every tag) · `versionCreator("simple")` — caught by a predicted-red `currentVersion` run: axion's default decorated snapshots with the BRANCH NAME (`0.1.1-axion-release-SNAPSHOT`); simple keeps snapshot pins branch-independent and CI's detached-HEAD tag checkout deriving the bare version · axion's own `release`/`markNextVersion` tasks NOT used (axion reads tags, humans write them — the interactive-task rejection upheld). Verified: branch position → `0.1.1-SNAPSHOT` (0.1.0 tag found+incremented, prefix proven); release position → bare `9.9.9` via throwaway LOCAL tag (never pushed — tags only trigger CI on push; deleted after); full build green. Cost accepted: builds now require the git repo. Facts: local snapshots read `0.1.1-SNAPSHOT` not `0.2.0-SNAPSHOT` — `./gradlew currentVersion` gives the mavenLocal pin; the pushed tag alone decides the release number. **Runbook now (supersedes rung 1's): a release = `git tag messaging-vX.Y.Z origin/main && git push origin messaging-vX.Y.Z`** — no release PR, no bump commits, no `HEAD~1`; CI checks out the tag, axion derives the bare version, guard passes, artifacts publish.

**✅ `messaging-v1.0.0` + `messaging-v1.0.1` RELEASED (2026-08-16) — the ladder's combined green-path proof, delivered:** user executed the one-command runbook twice within 15 minutes (1.0.0 on the checkpoint-record commit; 1.0.1 on the PR #15 typo-fix commit — `releAW` → `release`, a review miss visible in the Actions list since PR #11). Both runs green: guard's checks 3/4 passed on CI for the first time (`fetch-depth: 0` reasoning confirmed), axion derived the bare version on a detached-HEAD tag checkout (no branch decoration — `versionCreator("simple")` confirmed), all four artifacts published by the auto-minted token. NOTE the version jump 0.1.0 → 1.0.0 skipped the planned 0.2.0 and superseded the stay-0.x note (see locked decisions). Current consumer coordinate: `platform("io.github.siloverse:messaging-bom:1.0.1")`. Snapshots past the tag derive `1.0.2-SNAPSHOT`. Release machinery now qualifies as PROVEN — the extraction precondition is met.

**Then / next (direction = user's call):** extract the machinery to siloverse-build — now UNBLOCKED, the proven-precondition is met (release guard as reusable task, the axion `scmVersion` block, `siloverse.platform` convention for BOMs, the release workflow as a reusable workflow — one machine, moves together; siloverse-build repo/session owns the work, this repo's side = delete local guard + apply convention with `tagPrefix = "messaging-v"`) · first real consumer: a silo depends on `platform("io.github.siloverse:messaging-bom:1.0.1")` from GitHub Packages (validates the README's copy-paste path with real coordinates) · retention janitor · parked refinements on measured need.

**Parked:** topology migrations (queue arguments are IMMUTABLE on a live broker — changing them, e.g. adding dead-letter args to already-deployed queues, fails with 406/config error; needs a migration story when a deployed consumer upgrades) · retry-with-backoff ladder for consumers (TTL wait-queues + `x-death` attempt counting + minutes-scale cutoff; v1 policy is redelivered-flag two-attempt → DLQ) · DLQ alerting + operator replay · consumer concurrency/prefetch (QoS) · `MessageNameRegistry.classOf(String)` reverse lookup (consumer chapter: delivery carries `message_type`, consumer needs wire name → class for deserialization; builder already keeps the reverse map, currently discarded at freeze) · cross-service command double-binding (registry cardinality is per-service; nothing physically stops a second service binding a queue to a command's exchange → fanout would double-execute; contract jar is the social contract, broker won't police it) · after-commit dispatch · `MessageContext` (param index reserved) · `Request` revival · NATS adapter #3 · batch consumers · inbox/idempotency (now motivated: at-least-once ⇒ dedup by `message_id`; UNIQUE on message_id IS the mechanism there) · static-analysis warning for `AsynchronousBus` inside `@Transactional` scope · multi-instance relay (`FOR UPDATE SKIP LOCKED`) · CDC/Debezium relay (the industrial upgrade that replaces polling — adopt only at scale that demands it) · partial index `WHERE published_at IS NULL` (relay's need, add when measured).

## Working style

User types in IDE (Claude applies changes only on explicit ask); why-before-what; deliberate red tests with predicted failure reasons stated BEFORE running; design questions before answers; Claude nags unanswered deliverables; one logical change per commit.
